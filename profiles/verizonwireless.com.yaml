name: Verizon Wireless
password:
  rules:
    length:
      min: 8
      max: 20
    contain:
    - numbers: 1
    - letters: 1
    blacklist:
    - spaces
    - special characters (*, &, $, etc.)
https: partial
notes:
- HTTPS is only supported for login pages.
