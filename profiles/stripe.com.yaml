name: Stripe
password:
  rules:
    length:
      min: 1
  reset:
    url: https://manage.stripe.com/reset
    accept: email
  change:
    url: https://manage.stripe.com/account
    reauth: password
https: enforced
