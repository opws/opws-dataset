password:
  rules:
    length:
      min: 8
      max: 16
    must:
    - contain at least 1 letter and 1 number
    mustnot:
    - contain more than 7 numbers
    - contain more than 2 consecutive identical characters
  reset:
    url: https://secure4.billerweb.com/csg/inetSrv
    notes: Reset must be triggered through the main page's links.
https: only
