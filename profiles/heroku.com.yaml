name: Heroku
password:
  rules:
    length:
      min: 8
  reset:
    url: https://id.heroku.com/account/password/reset
    accept: email
  change:
    url: https://dashboard.heroku.com/account
    reauth: password
https: enforced
